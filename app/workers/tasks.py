# tasks.py — Celery tasks removed
# Image generation is now handled synchronously in app/api/routers/generate.py.
# If async/background generation is needed in the future, re-implement tasks here
# with celery + redis as the broker/backend.
