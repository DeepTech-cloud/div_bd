# workers/ — Celery & Redis removed
# Background task processing was removed since the generate endpoint is now synchronous.
# If async generation is needed in the future, re-add celery + redis here.
