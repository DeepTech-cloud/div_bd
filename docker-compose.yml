version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - media_data:/app/media
    environment:
      - UPLOAD_DIR=media
      - BASE_URL=${BASE_URL:-http://localhost:8000}
      - GEMINI_API_KEY=${GEMINI_API_KEY}

volumes:
  media_data:
